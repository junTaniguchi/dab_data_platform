{
  "datasets": [
    {
      "name": "pii_access_ds",
      "displayName": "pii_access_ds",
      "queryLines": [
        "-- 【要求仕様からの補正】system.access.audit はUnity Catalogのメタデータ操作\n-- （createTable/getTable等）は記録するが、行レベルのSELECT実行そのものは\n-- 記録しない（実機のaction_name一覧で確認済み）。実際にテーブルへSELECTした\n-- 記録は system.query.history の方に残るため、こちらを使う。\n-- 普段そのテーブルに触れないユーザーや、直近90日に登場しなかった新規\n-- アクセスユーザーを優先的に確認する。\nSELECT\n  executed_by,\n  start_time,\n  statement_text\nFROM system.query.history\nWHERE statement_type = 'SELECT'\n  AND lower(statement_text) LIKE '%customer%'\n  AND executed_by NOT IN (\n    SELECT DISTINCT executed_by\n    FROM system.query.history\n    WHERE start_time BETWEEN current_date() - 90 AND current_date() - 1\n  )\nORDER BY start_time DESC"
      ]
    }
  ],
  "pages": [
    {
      "name": "main",
      "displayName": "監査ログ（PIIアクセス＋新規ユーザー）",
      "pageType": "PAGE_TYPE_CANVAS",
      "layout": [
        {
          "widget": {
            "name": "widget1",
            "queries": [
              {
                "name": "main_query",
                "query": {
                  "datasetName": "pii_access_ds",
                  "fields": [
                    {
                      "name": "executed_by",
                      "expression": "`executed_by`"
                    },
                    {
                      "name": "start_time",
                      "expression": "`start_time`"
                    },
                    {
                      "name": "statement_text",
                      "expression": "`statement_text`"
                    }
                  ],
                  "disaggregated": true
                }
              }
            ],
            "spec": {
              "version": 2,
              "widgetType": "table",
              "encodings": {
                "columns": [
                  {
                    "fieldName": "executed_by",
                    "type": "string",
                    "displayAs": "string",
                    "title": "実行者"
                  },
                  {
                    "fieldName": "start_time",
                    "type": "string",
                    "displayAs": "string",
                    "title": "実行時刻"
                  },
                  {
                    "fieldName": "statement_text",
                    "type": "string",
                    "displayAs": "string",
                    "title": "SQL文"
                  }
                ]
              },
              "data": {
                "queryName": "main_query"
              },
              "frame": {
                "showTitle": true,
                "title": "監査ログ（PIIアクセス＋新規ユーザー）"
              }
            }
          },
          "position": {
            "x": 0,
            "y": 0,
            "width": 6,
            "height": 6
          }
        }
      ]
    }
  ]
}